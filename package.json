{
  "name": "chatv1",
  "private": true,
  "scripts": {
    "cf:frontend:install": "npm --prefix frontend install",
    "cf:frontend:build": "npm --prefix frontend install && npm --prefix frontend run build",
    "cf:backend:install": "npm --prefix backend install",
    "cf:backend:check": "npm --prefix backend install && npm --prefix backend run check:worker",
    "cf:backend:deploy": "npm --prefix backend install && npm --prefix backend run deploy:worker"
  }
}