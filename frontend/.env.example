# Leave empty in development — the Vite dev server proxy handles it.
# In production (Cloudflare Pages), set this to your Railway backend URL:
# VITE_API_URL=https://your-app.railway.app
VITE_API_URL=
